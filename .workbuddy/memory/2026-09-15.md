# 2026-09-15 工作日志

## 讲义修订：01-数据结构与算法
- 用户读 `docs/01-数据结构与算法.html#h-9`（完全二叉树顺序存储下标关系）时，发现「左孩子=2i」公式里的 **i 从未显式定义**，只有第 390 行「下标从 1 开始」和验证句的「B 是 2 号」隐含，读起来跳了一步。
- 已在源 md 第 400 行补一段「公式里的 i 是什么」，明确：i = 当前节点在数组里的层序编号（从 1 开始，根 A=1），算谁的孩子就把谁的编号代入 i。改完 `npm run build`（PowerShell 调托管 Node）同步了 docs/，HTML 第 539 行已含新内容。
- 备注：本机 Bash 的 PATH 被 WorkBuddy shim 搞坏（cat/tail/dirname 均 not found，cd 报 null directory）；构建一律改用 PowerShell 调 `C:\Users\10988\.workbuddy\binaries\node\versions\22.22.2-3\node.exe tools/build_html.mjs`。

## 01 讲义大扩写：第三节「图」为主，连带体检补齐查找/排序/算法设计（参考 hello-algo）

**用户诉求**：第三节「图」要像第二节那样参考 hello-algo 补全；并建议借这个项目体检"哪些章节/节点写得不够周全"，一并补。

**参考项目落地**：`git clone --depth 1 --filter=blob:none --sparse https://github.com/krahets/hello-algo.git .refs/hello-algo` 后 `git sparse-checkout set docs`（只取 docs 文本，约 43MB）；`.refs/` 已加进 `.gitignore`（不入库），并在 AGENTS.md 新增「开源参考项目（`.refs/`）」小节说明用途与纪律（只借结构与讲法、不整段搬运、不考的不搬）。

**扩写结果（`软考学习/01-数据结构与算法.md`：930 行 → 1885 行）**
- **第三节 图：51 行 → 528 行**。新增 3.0 术语/边数/度数和定理（含"连通≠完全"反例与连通分量图）、3.1 邻接矩阵 vs 邻接表（G1 双存法、无向矩阵对称判别、n+e / n+2e 考点、十字链表/邻接多重表）、3.2 DFS/BFS 逐步演算（同一张 G1 走出 A B D C E 与 A B C D E，含"入队即标记 visited"陷阱、非连通图需按连通分量起多次遍历）、3.3 Prim/Kruskal 双算验证（G2 两者都得 10）、3.4 Dijkstra 逐轮松弛表（含"直达边 6 被绕道 5 取代"）+ 三点负权反例 + Floyd 对照、3.5 拓扑排序入度法演算、3.6 关键路径四量表完整演算（G4：总工期 13、余量逐个算、四条自检）、3.7 六块内容"一句话抓手"小结表。
- **第四节 查找：20 行 → 150 行**。补五种查找的"前提条件"定位表（明确"链表不能二分"）、分块查找（两步 ASL 相加、s=√n 最优 → ASL≈√n+1）、**二叉排序树**（中序递增、插入必是叶子、删除三种情况含中序前驱顶替演算、ASL 数层数、有序插入退化成单支）、散列表扩充（除留余数法 p 取"≤表长的最大素数"、线性探测堆积的**粘连反例** 9/17/25/3 vs 链地址法 ASL 2 → 1.75）。
- **第五节 排序**：新增 5.4「三种过程亲手走一遍」——快排挖坑法全流程表、建堆（从 ⌊n/2⌋ 往前，含"换下来的元素要接着下沉"）、取堆顶一趟、归并三趟（趟数 ⌈log₂n⌉）。
- **第六节 算法设计与分析：28 行 → 190 行**。算法五特性（输入可 0、输出至少 1）+ 好算法标准；五策略各配可手算例子：汉诺塔 T(n)=2ⁿ−1、0-1 背包填表（含**反向追方案**验证）、贪心两个反例（找零 {1,3,4} 凑 6、0-1 背包 80 vs 98）+ "可分割才用贪心"、N 皇后回溯骨架、分支限界对比；复杂度数法表（嵌套相乘/倍增取 log/递推式）、空间复杂度与递归三要素。
- **新增「补充题（第三节 图 专项）」3 题**（MST / 关键路径 / 遍历与度数）+ 答案，按 AGENTS 规则 3 追加在精华题之后、未动原编号，答案附在「答案与解析」末尾。

**工具与踩坑**
- 用 `.refs/patch.mjs` 按标题定位做整段替换（内容分片写在 `.refs/parts/*.md` 再拼接），避免超长 Edit；`.refs/splice-section3.mjs`、`fixlinks.mjs`、`fixbrand.mjs`、`preview.mjs` 都留在 `.refs/` 便于复用（均已 gitignore）。
- **踩坑 1**：`---\n\n**下一课**` 这类跨行锚点在 CRLF 工作副本里匹配不到，插入锚点必须用**单行串**（如 `**下一课**`）。
- **踩坑 2（已固化进 AGENTS 自检清单第 9 条）**：构建脚本给标题的 id 是**顺序号** `h-N`，md 里手写 `](#小节)` 页内锚点全部失效 → 本次 4 处改为纯文本引用（"见第二节 2.5 平衡二叉树"）。
- **品牌名清洗**：正文里 4 处「三色笔记」改成「参考资料/固定考点」。
- **渲染核对方式（可复用）**：`.refs/preview.mjs` 从 `docs/01-…html` 里按 `h-N` 切出指定区间，配 `docs/style.css` 在 `.refs/` 下独立成页，再无头 Chrome 截图（1440 浅色 + 1200 深色）。用 `#h-N` 直接给整页加锚点截图会拍到空白，不可靠。

## 新需求：遍历「绕轮廓」交互动画（设计中，未写码）
- 用户想为 2.2 遍历节做一个 CSS 动画页面：walker 沿树轮廓走一圈，把每个节点的三次触碰点（①左下去=前序 ②正下=中序 ③右上=后序）做成描边状态，并**与讲义现有的 16 行绕圈表双向联动**（高亮行 ↔ 点亮节点灯 ↔ 推进轮廓描边）。
- 已交付 ASCII 原型 + 联动规格（步号→(节点,状态) 映射、自动播放/点行跳转/点节点反查三行）。三态色板沿用讲义：橙#EF9F27/绿#1D9E75/红#E24B4A。
- 约定结构：新建 `interactive/traversal-contour/`（index.html + style.css + app.js），**不写入 docs/01 原文**，由讲义 2.2 节用相对链接 `traversal-contour/index.html` 跳转。
- 线上部署待办：改 `tools/build_html.mjs` 把 `interactive/` 拷进 `docs/`（GitHub Pages 只发 docs/）。
- 流程：用户先拿原型找设计师出 style.css → 我再写 index.html + app.js → 挂链接 + 接入构建。**本轮回合只出设计稿，未写代码**。

## 设计稿交付：绕轮廓遍历动画（UiDesigner 角色）
- 用户切到 UiDesigner expert 并说"根据以上文档开始设计"，遂进入设计阶段（非写码）。
- 已读 `tools/style.css` 对齐设计令牌（暖纸底/深青主色/琥珀/红，深浅双主题）。
- 产出自包含设计预览 `design/traversal-contour/preview.html`：复用讲义同一套令牌，三态=前序金/中序青/后序红；含树 SVG（轮廓描边 dashoffset 动画 + walker 脉冲）、节点状态灯网格（每节点①②③）、16 行步骤表（行高亮+前中后单元格着色）、控制条、图例、折叠式设计说明（交付编码阶段的组件/动效/联动/响应式规格）。快照定格第 7 步（E·前序）。
- 该文件是静态设计预览（CSS 动效示意），最终由 `interactive/traversal-contour/` 三件套实现，讲义 2.2 节相对链接跳转。尚未写 app.js/真实联动逻辑。

## 交互页已落地：绕轮廓遍历动画（真实可跑）
- 用户反馈 design/preview 的轮廓「路径线条不够圆润」，要求改成「贴着每个节点自身的左/底/右弧线走」：每个节点画成圆，walker 沿该节点自身的弧走——①左半圆=前序、②下半圆=中序、③右半圆=后序，自然圆润且与讲义 16 步一一对应。
- 已写 `interactive/traversal-contour/` 三件套：`index.html`（SVG 树 6 圆节点 + 16 段沿节点弧线的进度路径）、`style.css`（复用讲义令牌，前序金/中序青/后序红，深浅双主题）、`app.js`（几何 + 逻辑）。
- 几何要点：16 段路径用 Bézier 半圆（避免 SVG arc sweep 方向画错），段与段端点首尾相接成闭合环；walker 用 `<g>` transform 平移定位（CSS transition 平滑跟随）；每段按 pass 着色，三色轨迹可同时显示。
- 联动：自动播放（步 1→16，play/pause/reset + 调速）；点表格行→跳步；点节点（SVG 或灯卡）→反查并高亮它的三次所在行（peer 样式）。严格复用讲义 §2.2 的 16 行动作文本作单一数据源。
- 接入构建：① 在 `01-数据结构与算法.md` 表后加相对链接 `interactive/traversal-contour/index.html`；② `tools/build_html.mjs` 末尾新增 `copyDir` 把 `interactive/` 递归拷进 `docs/`（GitHub Pages 只发 docs/，故必须拷贝）。构建已验证：exit 0，`docs/interactive/traversal-contour/{index.html,style.css,app.js}` 均生成，01 阅读版第 711 行含跳转链接。
- 备注：`design/traversal-contour/preview.html` 已被真实交互页取代，可保留作设计记录或删除（本次未删）。
- 构建命令（本机 Bash PATH 被 shim 搞坏，改用）：PowerShell 或 `Bash: <托管node> tools/build_html.mjs`（Bash 的 cat/tail 被 shim 吞，但 node 绝对路径可直接跑）。

## Bug 修复：节点边缘高亮看不见
- 用户反馈「边缘亮没有体现出来」。根因：`app.js` 把 16 段轮廓 `.seg` 插到**所有节点之前**（节点底层），而节点圆是**不透明填充**，于是「walker 沿节点自身弧线走」那段轮廓被节点填充盖住，只有节点间的连接线可见 → 节点边缘看不出上色。
- 修法：在 `interactive/traversal-contour/` 加「节点边缘弧高亮」——给每个节点在**圆边之上、walker 之下**生成三段路径（左半圆=前序金 / 下半圆=中序青 / 右半圆=后序红），几何用 r×0.5523 的半圆周 Bézier，与 `.seg` 弧段完全重合。按步 1..k 累加点亮、`litSet` 控制；当前步的弧加 `.cur` 用 `drop-shadow(currentColor)` 发光。
- CSS 新增 `.edge-arc`（opacity 0→1 过渡，pointer-events:none 不挡节点点击）；`goToStep` 第 3.5 步刷新所有弧的 on/cur。
- 已重建并验证：`docs/interactive/traversal-contour/{app.js,style.css}` 时间戳 16:19，内容含新逻辑。

## 精进：边缘高亮精确落在节点圆周上
- 用户进一步要求「边缘高亮是节点的圆的边缘本身」，原版用贝塞尔近似半圆（r×0.5523）会相对真圆极微内缩，加描边宽度后看起来像节点圆内侧套了一圈彩环，而非圆边在发光。
- 修法：`arcD` 改用**精确 SVG 圆弧命令 `A`**（半径/圆心与节点圆完全一致）：左半圆 `M cx,cy-r A r,r 0 0 0 cx,cy+r`、下半圆 `M cx-r,cy A r,r 0 0 0 cx+r,cy`、右半圆 `M cx,cy+r A r,r 0 0 1 cx,cy-r`。高亮弧与灰边是同一道圆周，宽度 4.5 盖住 2px 灰边，当前步 `.cur` 加宽到 5.5 并 `drop-shadow` 发光 → 视觉上就是「节点圆的边缘在变色发光」。
- 已重建并验证：docs 副本时间戳 16:33:54，含精确 A 圆弧代码。

## 全库体检与「讲义扩充规划」+ 批次 0 纠错
**体检方法（可复用）**：6 个只读 Explore 子智能体分组并行（02/03、04/05、06/07、08/09、10/11、12+99），每个提示词自包含（项目背景 + AGENTS 三节规范 + 大纲口径 + 四项任务：大纲「学习范围」逐条核对 / 深度自检六条 / `pdf/text/` 对照找漏点 / 抽 3~5 题亲自复算）；子智能体只报线索，**硬错由主会话逐条打开原文复核**。
**产出**：`讲义扩充规划.md`（仓库根，**不进 docs/**——构建脚本只扫大纲 + `软考学习/*.md`，放根目录才是工作文档）——含全库体检总表、6 处硬错、4 处大纲硬缺口、逐课缺口清单、5 批执行路线、验收标准。
**关键结论**：① 答案可靠性分化——09（12 题）、12（14 题）、99（17 选择 + 2 案例）**零错**；02 精华题 4 候选键错（标 AE，应为 E）。② 4 处大纲硬缺口：04 缺 OOA/OOD 整节、07 缺「程序设计语言基本成分」整节、03 缺模块设计原则与 DFD 补实体/存储/加工、06 的 HTML 常识过薄。③ 跨文件冲突：大纲 L126 专利年限旧口径 vs 10 讲义新法。④ **资料盲区**：`pdf/text/` 12 份里没有信息安全、知识产权、数学多媒体、网络、数据库的独立章节，这五课只能靠大纲 + 真题卷。
**批次 0 纠错（已完成，共 9 处）**：
1. `02` L476 候选键 **AE → E**（并补"最小超键"判别 + 反查 A 推不出 E）
2. `02` L321 课后题 15 选项 D 残缺 → `D. ROLLBACK`
3. `06` 三次握手表：状态列错位（把 B 的 SYN-RCVD 填进"对方"列）→ 改为「本步的状态变化」单列 + 标注发送方在步骤间换人；**新增 seq/ack 数字表**（x=100/y=200 → ack=101/201）+ 规律"ack = 对方 seq + 1"
4. `06` L218 CIDR 聚合反例：删掉自相矛盾的"24−6 = /22 不到"→ 改为"共同前缀 /22 + 两关判据（数量够、起点块对齐）"完整推理
5. `10` L22/L27 法人作品发表权口径 → 发表权锚"**创作完成后** 50 年"、财产权锚"首次发表后 50 年"，并加记忆抓手
6. `10` L35 一般职务作品 → 拆成"单位的权（业务范围内优先使用，**无期限**）"与"作者的限（完成 2 年内不得许可第三人以相同方式使用）"+ 易错点段
7. `10` L52 应试动作、L339 解析 → 补"**软件是特例**：条例统一按首次发表后 50 年"
8. `软件设计师考点大纲.md` L126 → 实用新型 10 / **外观设计 15**（2021 新法口径）
9. `10` L29 新增"没发表看创作完成，发表了看首次发表"的记忆抓手
**构建与校验**：`node tools/build_html.mjs` → 17 页，HTML 逐项校验通过。**踩坑（重要）**：本机 Bash 的 `grep` 对"中文 + `.\{0,N\}` 量词"组合会产生**假阴性**（文件里明明有却报无匹配），核对 HTML 必须用内置 Grep（ripgrep）。

## 全库体检 + 扩写规划（02~12 与模拟卷，对照 01 的基线）

**用户诉求**：除已扩写的 01 外，其他课的 HTML / 讲义也照同样标准对比规划一遍。

**做法**：派 6 个只读子智能体（Explore）分组并行体检 12 份文件，每组按同一套判据：① 逐条核对 `软件设计师考点大纲.md` 的「学习范围」；② 深度自检（有结论无演算 / 公式缺三件套 / 缺反例 / 缺应试动作 / 一句话带过学不懂 / 对比表缺列）；③ 在 `pdf/text/` 里检索该考点章节找漏点；④ 抽 3~5 道计算题**亲自复算**。主会话再亲自复核了 6 处关键点。

**产出**：仓库根目录新增 `讲义扩充规划.md`（工作文档，**不进 docs/**——构建脚本只扫大纲与 `软考学习/`，不碰根目录 md），并在 `README.md` 目录树加了一行。

**核心结论**
- A 层硬错 6 处（已逐条亲自核实原文）：① `02` L476 精华题 4 答案「候选键 = AE」**错**，其解析自己推出 E 闭包 = ABCDE，应为 **E**（AE 是超键）；② `02` L321 课后题 15 的 D 选项文字残缺；③ `06` L254-258 三次握手表第 2 行状态列错位（B 的 LISTEN→SYN-RCVD 填进了"对方"列）；④ `06` L218 CIDR 聚合反例推理句自相矛盾（"24−6 = /22 不到"）；⑤ `10` L22/L27 把法人作品**发表权**也写成"首次发表后 50 年"（应为**创作完成后** 50 年）；⑥ `10` L35 一般职务作品把"单位业务范围内优先使用（无期限）"与"作者 2 年内不得许可第三人"并成一条。
- B 层大纲硬缺口 4 处：`04` 缺「面向对象分析与设计过程」整节、`07` 缺「程序设计语言的基本成分」整节（资料中考频最高）、`03` 缺「模块设计原则」整块、`06` 的 HTML/浏览器常识只写了 33 行。
- C 层深度不足普遍存在：`09`（CRC 无竖式、海明纠错未展开、移码无动机、可靠性三公式缺）、`03`（McCabe 不会从流程图数 E/N、白盒最少用例）、`05`（段页式位数、磁盘单块时间、索引逻辑块号三个真题型缺算例）。
- **答案可靠性抽查**：`09`（12 道）、`12`（14 道）、`99`（17 道选择 + 2 道案例）**零错误**；只有 `02` 有 1 处真题答案错。`99` 结论是**可直接模考**。
- **跨文件不一致**：`软件设计师考点大纲.md` L126 写"实用新型和外观设计 10 年"（修法前口径），与 `10` 讲义的"实用新型 10 / 外观设计 **15**"打架——讲义是对的（2021 新《专利法》），建议改大纲。
- **资料盲区**：`pdf/text/` 12 份资料里**没有信息安全、标准化知识产权、数学与多媒体三个独立章节**，也没有网络与数据库章节（已逐文件确认）；这五课只能靠大纲 + 真题卷校准。

**下一步**：批次 0（修 6 处硬错 + 同步大纲年限）→ 批次 1（04/03/02）→ 批次 2（07/06/09/05）→ 批次 3（11/10/08/12）→ 批次 4（模拟卷补 3 组题）。

## 精进：边缘弧改为钟面三等分（修掉右边缘糊色）
- 用户反馈「右边缘写得不好」。根因：`arcD` 旧版把圆周切成左半圆(12→6)/下半圆(9→3)/右半圆(6→12)，相邻段在「9→6」与「6→3」两段**重叠**；点亮是累加的，后亮的盖先亮的，于是右半圈下半截(6→3)本该是中序青，被后序红覆盖，右边缘与底边在交界处糊成一片、分界不清。
- 修法：把圆周**按钟面三等分、三段互不重叠**——左 12→8 点（经 9 点左缘，前序金）/ 底 8→4 点（经 6 点最低点，中序青）/ 右 4→12 点（经 3 点右缘，后序红），各 120°，边界在 8 点、4 点、12 点。用 `RANGE={pre:[12,8],in:[8,4],post:[4,12]}`，`P(h)` 由钟面小时换算 SVG 弧度 `(h*30-90)`，每段 `A r,r 0 0 0` 沿递减方向连续绕整圆，首尾相接无重叠。
- 这样左/底/右边缘各自是一段纯色弧、精确贴在节点圆周上，不再因重叠而串色。同时把顶部几何注释、SEG_D 里「7:30/4:30/左半圆」等旧措辞同步对齐。
- 已重建并验证：docs 副本时间戳 16:48:08，含 `pre:[12,8]` 新分法；构建日志确认 docs/interactive/ 已更新。

## 批次 1 第 1 课：04 面向对象与设计模式 扩写（336 → 826 行）

**素材来源**（比规划时预估的更好）：`pdf/text/2026年下半年软件设计师三色笔记.txt` 的面向对象章节（L2014-2730）非常完整——含 OOA/OOD 流程、OOD 七大原则与六条包原则、13 种 UML 图定义、每个设计模式的**角色图与角色名**；`经典100题` 有 OOD 原则真题（共同重用原则）；`案例模拟题合集` L110-175 有近真题形态的 UML 案例（填 C1-C4 类名 / M1-M4 方法名 + 模式识别），已改写为课后案例题。

**扩写结果**
- 第一节：新增 1.2 类的三种类型（实体/边界/控制，口诀"实体存数据、边界接用户、控制管流程"）、1.4 多态四形态（过载/参数/包含/强制）、1.5 动态绑定三条件；1.1 补对象三件套（属性+方法+对象 ID）。
- **第二节「面向对象分析与设计过程」整节新增**（大纲明列、原版全缺）：OOA 四步（认组拉定）、OOD 五步、**OOD 七大原则**（每条配反例 + 依赖倒置真题原话）、六条包原则（共同重用/共同封闭题眼）。
- 第三节 UML：10 图 → **13 图全景**；类图补可见性符号 + 多重性"逐端问"读法；用例图补**建模流程四步** + 典型/备选事件流读法；顺序图与通信图、活动图（分叉 vs 分支）、状态图（**五要素 + 四类事件 + entry/exit/do + 顺序 vs 并发子状态**）、构件图（**整圆供 / 半圆需**）、部署图（立体长方体）各自成节。
- 第四节：新增 **4.4 模式角色名**（19 个模式的角色链，直服案例题第五题填空）、类模式仅 4 个的作用范围维度、代理三细分、易混组补"策略 vs 状态""工厂方法 vs 抽象工厂""命令 vs 备忘录"。
- 第五节案例题专攻：3 → 4 个套路，新增"由事件流动词反推方法名"、"题干 → 模式"识别表、"模式小问答题模板"（模式名 + 原因 + 点名原则）。
- 课后题 15 → 22、精华题 6 → 10，答案与解析同步补齐。

**两处构建不报错的渲染缺陷（重要，已固化进 AGENTS 自检清单第 10–12 条）**
1. **4 处裸写 `<<include>>`/`<<extend>>` 被浏览器当未知标签吞掉** → 页面上整段不显示（第 6 题两个选项直接消失），而 `npm run build` 不报错。修法：md 里构造型**必须包进反引号**；检查命令 `grep -o '<\(include\|extend\|component\)' docs/xx.html`。
2. **4.4 角色表首列被压成中文竖排**（"抽/象/工/厂"）→ 长文本塞多列表格的通病，改用**分组列表**重写。
3. 附带：`◗`（U+25D7）在中文等宽字体无字形 → 渲染成豆腐块，改文字描述。

**工具改进**：`.refs/preview.mjs` 改为可指定页面（`node .refs/preview.mjs "<页码>" <起> <止> <light|dark>`）并给预览页加"正文固定 44rem"样式——缺侧栏时 grid 会把 main 挤成窄列、表格列宽失真，会误判成页面 bug。无头 Chrome 的 `#h-N` 锚点截图不可靠，定位一律走预览页。

**状态**：04 讲义 + AGENTS.md + 讲义扩充规划.md（批次 1 标 04 完成）已改，docs/ 重建 17 页；三档宽度（1440/1140/880）+ 深色截图核对通过。**尚未提交**（与批次 0、traversal-contour 改动一并待提交）。

**下一课**：批次 1 的 `03-软件工程基础`（398 行，缺「模块设计原则」整块、DFD 只会补数据流不会补实体/存储/加工名）。

## 遍历动画迭代：行走改用二叉树边高亮（去掉自定义轮廓边）
- 用户要求：A↔B 行走时**不要画自定义的轮廓边**，而是**直接用二叉树的结构边来高亮**表示"正在走这条边"；节点圆边的三段高亮（前/中/后）保留。
- 改动 `interactive/traversal-contour/` 三件套：
  - `index.html`：给 5 条 `<line class="edge">` 加 `data-edge="A-B|A-C|B-D|B-E|C-F"`；删除原 `#base` 轮廓底图路径。
  - `app.js`：删掉 `SEG_D`/`.seg`/底图逻辑与 `endXY`/`segEls`；新增 `edgeEls`（按 data-edge 索引）、`nodeBoundary(node,to)`（walker 停在「当前节点圆边、朝向即将前往的邻居」处，使其滑动时恰好沿二叉树边）；`STEPS` 每步加 `to` 与 `edges` 字段；`goToStep` 改为高亮**当前步涉及的树边**（颜色随当前 pass：金/青/红），walker 加 `.pre/.in/.post` 类使自身颜色随阶段。
  - `style.css`：删 `.seg`/`.contour-base` 规则；新增 `.edge.active{stroke-width:4;drop-shadow}` + `.edge.active.pre/in/post` 上色；walker 改为按阶段变色（`.walker-g.pre .walker{fill:var(--c-pre)}` 等）。
- 几何校验：walker 停靠点取节点圆边朝向邻居的切点，父→子滑动方向与该树边平行，视觉即"沿边走"；分支点（A/B）处因换邻居会有短暂绕节点摆动，由节点圆边弧（前/中/后）点亮解释，符合"绕一圈"语义。
- 已重建并验证：exit 0，`docs/interactive/traversal-contour/` 三件套含新逻辑、无 `SEG_D/segEls/#base` 残留。

## 01 讲义 2.4「哈夫曼树与哈夫曼编码」由纯文字定义改写为图解版

**用户诉求**：该节原是 11 行纯文字定义，"建议加上 ASCII 解释和几个示例解释必要的关键词定义"。

**改法（`软考学习/01-数据结构与算法.md` 第 516 行起，11 行 → 约 175 行）**
- 新增「它解决什么问题」动机段：定长编码付同样房租 → 变长编码引入歧义 → 哈夫曼是同时满足"短码 + 无歧义"的答案。
- 新增「先认五个关键词」：路径长度（边数，根为 0）/ 权 / **WPL（只算叶子、乘边数）** / 哈夫曼树 / 前缀码，每个配 ASCII 三叶树示例（WPL = 8×1+5×2+2×2 = 22，非叶节点和 7+15 = 22 互验）。
- 新增「构造过程三时刻」图：时刻 0 森林 → 时刻 1/2/3 每轮树形 + 剩余森林，**用 `( )` 标原始叶子、裸写数字标内部节点**，解决权值撞车（两个 3）时的歧义。
- 新增「从树到编码」树图 + **含"从根走的路径"列**的编码表（4→`1`、3→`01`、1→`000`、2→`001`；WPL 19 两种算法互验）。
- 新增第二个"像真题"的例子 {5,9,12,13,16,45}：5 轮合并流水 → 树形 → 编码表 → WPL = 224（非叶节点和 14+25+30+55+100 互验），并点出"频率越大码越短""收益来自不均衡（300 → 224，省 25%）"。
- 新增「前缀码为什么必须」：反例 `e=0,a=1,b=01,d=00` 收 `0100` 可读成"卡住"或 `bd` 两种；再用"叶子 = 编码终点"说明哈夫曼天然满足、也解释了"字符必须挂叶子"。
- 新增「四条必背结论」（2n−1 与无度为 1 的由来 / 权大离根近 / **树不唯一但 WPL 唯一、编码不唯一但码长分布唯一** / WPL = 非叶节点权之和）+「应试动作四步」+ 层次与路径长度的换算读法（路径长度 = 层次 − 1）。

**本次自己写出的两处错误（已修，并固化进 AGENTS 自检清单的新第 13 条）**
1. 关键词演示树画成了「右孩子为空」的畸形树——**哈夫曼树不允许度为 1 的节点**，等于用一张违反本节性质的图去讲本节。改为合法三叶树。
2. 反例推导写错：原写"第 2 轮挑 3 和 4 会得到根权 16"，重算后发现**错误树的根权同样是 10**，真正暴露问题的是 WPL = 20 > 19。已改成"根权一样但 WPL 更大"，并把"最后总数一样骗不了人，WPL 才是唯一判据"作为教训写出。

**ASCII 对齐的省事办法（可复用）**：等宽字体下斜线偏 1 格就显脱节、中文/全角按 2 宽算，肉眼数空格必错。用 Python 小脚本把整块图逐行打印 `len(line)` 与 `/ \` 的字符下标即可校准；本次 4 张图都是这么对齐的（脚本在系统 Temp，未入库）。

**构建与状态**：PowerShell 调托管 node 跑 `tools/build_html.mjs`（exit 0），`docs/01-数据结构与算法.html` 已同步，新小节拿到 `h-17` 起的顺序 id（因无页内锚点链接，冲掉旧 id 无影响）。**本次未提交**——工作区仍有批次 0/1、traversal-contour 等大量未提交改动（01/02/04/06/10 的 md、docs/、tools/、AGENTS.md、README、.gitignore），沿用"一并待提交"的约定保持不动。

## 01 讲义 2.5 平衡二叉树 / 2.6 B 树与 B+ 树：确认非重点后做「少量定义 + 图例」

**用户提问**：这两节是否是重点？若不是，加少量定义和 ASCII 图例辅助即可。

**考频查证（结论：都不是重点，2.5 比 2.6 略强一点）**
- `经典100题`（100 道真题风格精选题）：哈夫曼 **3 道**（第 4、48、90 题）、二叉排序树 1 道（第 76 题）、**AVL 0 道、B 树/B+ 树 0 道**。
- `三色笔记`（按考频编排的核心资料）：7 处"平衡"**全部是 DFD 的"数据平衡原则"**，与 AVL 无关；B 树零提及。
- `知识点集锦`：AVL 只有**一行定义**（"树中任一结点的左右子树高度之差不超过1"），与"查找二叉树""最优二叉树"并列在"特殊的树"下；B 树零提及。
- `默写本`：AVL 只有一个定义填空；**12 份资料里 OCR 全文搜不到"B 树"**。
- 大纲（`软件设计师考点大纲.md` L58）只把两者列在"学习范围"一行，无独立优先级。
- 旁证：`pdf/text/` 全域搜 `B\+?树|B－?树` 零命中 → 参考资料根本不收这个考点。

**改法**（`软考学习/01-数据结构与算法.md`：2.5 由 21 行 → 约 75 行，2.6 由 7 行 → 约 55 行）
- 两节开头都加**「先把考频说清」**段，明确"别背过程、只做这几件事"，把用户的复习时间导向 2.2 遍历 / 2.4 哈夫曼。
- **2.5** 补：BST 退化成斜链的动机段；两个必须先认识的定义（**AVL 树**双条件、**平衡因子 BF = 左子树高度 − 右子树高度**，合法值 −1/0/+1）；一张**正反例对照图**（合法树逐个标 BF vs 节点 8 的 BF=+2 越界）；**LL 右旋**与 **LR 两步旋转**各一套 ASCII 变换图（RR/RL 用"左右镜像"文字带过，控制篇幅）；收尾"考试怎么问"三问。
- **2.6** 补：**磁盘 I/O 动机**（100 万条记录二叉树高约 20 → 最坏读 20 次盘 → 把树变胖压低树高，"B 树是被磁盘逼出来的，不是为内存设计的"）；**m 阶定义**（孩子数上限、关键字数 m−1、除根外 ≥⌈m/2⌉ 个孩子、所有叶子同层）；**B 树 / B+ 树各一张结构图**（拓扑相同，差别只在"数据放哪、叶子连不连"，B+ 图带横向链表线）；对比表补一列**"查找路径"**；"数据库为什么用 B+ 树"两句；"考试怎么问"三问。

**自己写出的错误（已修，并补进 AGENTS 自检第 13 条）**
- 平衡因子的"高度"**正例按层数、反例按边数**，两套口径混用（正例写 BF(5)=2−2，反例写 BF(8)=2−0）。统一为**层数制**（单节点子树高 1、空子树高 0，教材算 BF 的通用口径），反例三个式子按此重算为 3−1、2−1、1−0，并把"同一小节多张图必须共用一套口径"作为第 13 条的新增子项固化。

**验收方式（可复用）**：`.refs/preview.mjs 20 22 light` 生成 2.5–2.6 局部预览（需先把 `docs/style.css`、`docs/app.js` 拷到 `.refs/`，否则无样式），再用无头 Chrome `--headless=new --force-device-scale-factor=2.2` 按 `h-20~h-21`、`h-21~h-22` 分段高清截图逐节目视核对：树形、斜线、公式全部对齐，⌈⌉ 与 − 渲染正常。**本机 Bash 不可用 head/ls/tail**，文件存在性判断一律用 Python 的 `os.path`。

**状态**：01 讲义 + AGENTS.md 已改，`docs/` 已重建（exit 0），HTML 抽查通过。**仍未提交**（与之前的批次改动一并待提交）。

## 批次 1 第二课：03 软件工程基础 398 → 1163 行（2026-09-15 晚）

**流水线（沿用 01/04，可复用）**：7 个内容分片（`.refs/parts/s03-1…7.md`，上一轮已写好但未拼接）+ 本轮新增 3 片（`s03-8` DFD 案例节、`s03-9` 补充题、`s03-10` 补充题答案）→ `.refs/patch03.mjs` 一次拼接。
- 拼接做法：用**精确标题串**定位锚点 → 记 `[from,to)` 区间 → 按 `from` **降序** splice；答案区用 `indexOf('### 案例题答案')` 之后的 `**问题 1（…` / `**问题 2**：` / `**问题 3**：` 三段**整段替换**（避免去匹配全角引号形态）。
- 拼接前先 `cp` 备份成 `.refs/03.bak.md`；脚本末尾自动打印标题结构 + 关键串计数自检。

**产出**：第一节改为「软件开发模型与方法」（方法 vs 模型两分、敏捷族六成员）；第三节新增 **3.1 模块设计六原则**（原讲义整块空白）；第四节扩成 4.1–4.11（静态动态两分、V 模型、桩/驱动模块、逻辑覆盖、数最少用例数、McCabe 数边数点、测试五原则）；第五至八节重写；第九节由 4 套路扩成 **9.0–9.9**（DFD 四元素 + 两条画法铁律 + 七套路 + 黑洞/奇迹/灰洞）。
追加「补充题（模块设计原则 / 测试计算 / 项目管理 / DFD 专项）」5 选择 + 1 案例及全部解析；原课后题 1–15、精华题 1–7 编号未动。

**顺手修掉旧案例题答案的口径错**：原答案「缺失数据流共 3 条」，但题干一条「加工读文件」的流都没画，按讲义自教的逐句扫描规则应为 **5 条**（补「订单文件→②待处理订单」「配送文件→③配送任务」）。已改写，并补两个易漏点：**「读文件」也是流**、**一句话可能对应两条流**；问题 2 答案同步纠正（加工③不需要读订单文件，说明里没这句）。

**渲染坑（AGENTS 第 12 条的又一次命中）**：9.7「三种错误」表首列 2 字被压成单字竖排（黑洞→黑/洞），与 04 角色表同类。**可复用判据**：表首列文字 ≤3 字、同表其他列有长句时**必然发生** → 改成分行列表重写，别只想着调列宽。

**验收**：自检扫描元叙述 / 品牌名 / 裸 `<<X>>` / 页内锚点**全部为 0**；构建 17 页通过；产物表格 36、代码块 12、答案折叠 1（确认补充题答案在折叠块内）；数据字典表的转义竖线 `[ | ]` 渲染正确；局部预览（9.0、9.7）+ 真实页 1440 三栏 / 880 抽屉深色截图核对通过。

**提交**：拆两个中文提交（内容层 `f218473` + 产物层 `9848192`）并推送 Gitee（`d8f569d..9848192`）。
