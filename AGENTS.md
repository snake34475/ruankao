# AGENTS.md — AI 协作指南

本仓库是一套「软考中级·软件设计师」自学资料库，由 AI 逐步生成、用户以本仓库为唯一学习资料备考（目标：2026 年 10 月 24—27 日考试，两科均 ≥45 分）。任何 AI 在本仓库工作时，必须遵守以下规范。

## 项目背景

- 用户**不看视频**，本仓库文件是唯一学习资料，讲义必须自足、通俗易懂。
- 资料按「一考点一文件」逐步生成：AI 每次只生成一个考点文件，用户学完并反馈后说「下一课」再生成下一个。（12 课与模拟卷现已全部生成；若用户再说「下一课」，先核对是否仍有未生成的考点，没有就如实说明，并转为修订 / 补题 / 答疑。）
- 阅读本文件后，先读 `软考学习/00-总览与进度.md` 确认当前进度，再读 `软件设计师考点大纲.md` 了解全局。

## 目录结构与文件职责

```
├── AGENTS.md                    ← 本文件：AI 行为规范
├── README.md                    ← 仓库首页（面向人，保持简短；新增文件后更新其目录树）
├── 软件设计师考点大纲.md          ← 全局大纲：考点范围、题型、分值、优先级（已定稿，非必要不改）
├── 软考学习/
│   ├── 00-总览与进度.md          ← 进度 TODO（checkbox）+ 学习方法 + 推进规则
│   ├── 01~12-<考点名>.md         ← 12 课讲义（讲义 + 课后题 + 精华题 + 答案与解析）
│   └── 99-考前全真模拟卷.md      ← 75 道选择题 + 5 道案例题，含答案解析
├── package.json                 ← 构建配置（唯一依赖 marked；node_modules 已 gitignore）
├── tools/
│   ├── build_html.mjs           ← Markdown → HTML 构建脚本（npm run build）
│   ├── style.css                ← 阅读版样式源码（构建时拷贝到 docs/）
│   └── app.js                   ← 阅读版交互源码（主题/大纲/进度/复制，构建时拷贝到 docs/）
├── design/
│   └── styleguide.html          ← 设计组件样张（引用 docs/ 样式与脚本，不参与构建）
├── .workbuddy/                  ← AI 会话记忆（每日工作日志 + 项目约定），会话自动维护，**勿删**
├── docs/                        ← HTML 阅读版（构建产物，随仓库提交，勿手改）
└── pdf/                         ← 本地参考资料库
    ├── *.pdf                    ← 12 份辅导资料原件（纯图片扫描件，约 152MB，已加入 .gitignore 不提交）
    └── text/*.txt               ← 各 PDF 的 OCR 提取文本（已去除品牌水印，随仓库提交；带「===== 第 N 页 =====」分页标记，可直接 grep 检索）
└── .refs/                        ← 本地参考仓库（hello-algo 等开源项目克隆，**已 gitignore、不提交**；用于补充讲义欠周之处、校准讲法）
```

## 考点生成顺序（固定，勿调换）

> 本列表只定义**顺序**，不记录进度：12 课 + 模拟卷共 13 份讲义目前已全部生成。
> 「学到哪了」只以 `软考学习/00-总览与进度.md` 的 checkbox 为准——不要在本文件里另维护一份勾选状态（曾出现本文件只有第 1 课打勾、而进度表实际已勾了 09/10/11/12/99，两处长期不一致）。

1. 数据结构与算法
2. 数据库系统
3. 软件工程基础
4. 面向对象与设计模式
5. 操作系统
6. 计算机网络
7. 程序设计语言基础
8. 信息安全
9. 计算机系统基础
10. 标准化与知识产权
11. 专业英语
12. 数学与多媒体
13. 收尾：考前全真模拟卷（75 选择 + 5 大题，含答案解析）

## HTML 阅读版（docs/）

- `软考学习/*.md` 与 `软件设计师考点大纲.md` 是**唯一真源**；`docs/*.html` 全部由 `npm run build` 生成，**不要手改 docs/ 下的任何文件**。
- 每次新增或修改课程 md 后，重跑 `npm run build` 同步 docs/（产物随仓库提交，纯文本小文件）。
- 样式与交互源码在 `tools/style.css`、`tools/app.js`（构建时原样拷贝到 docs/）；跨文件 `.md` 链接由脚本自动重写为 `.html`。
- 阅读版 UI 已实现：暖纸阅读主题 + 深浅色切换（localStorage，跟随系统）、★ 星级渲染成琥珀徽标、页首引用块升级为导语卡、「答案与解析」整段折叠为 `<details>`（默认收起）、顶部阅读进度条、回到顶部、代码块复制按钮、索引页按梯队分组的课程卡片。
- **页面骨架为三栏**：左栏＝总纲梯队目录（总纲/第一二三四梯队/收尾分组常驻，当前页高亮、已完成打勾、组内标注 `已学/总数`），中栏＝正文（44rem 单栏），右栏＝本页大纲（由正文 h2/h3 自动生成 + 滚动高亮）。窄屏塌缩：1080–1199px 时右栏隐藏、本页大纲由 `app.js` 移回左栏下方；<1080px 左栏整块变抽屉（顶栏「目录」唤出，总纲与本页大纲同抽屉）。
- 梯队分组的唯一定义在 `tools/build_html.mjs` 的 `TIERS` 常量，左栏导航与索引页卡片共用；增删课程或调整梯队顺序只改这一处。
- 设计参考与组件样张：`design/styleguide.html`（引用 `docs/style.css` 与 `docs/app.js`，不参与构建）。改样式只需改 `tools/style.css` 后重跑构建，样张会同步生效。
- 轻量约束：不引入任何外部依赖与 Web 字体（中文字体包体积过大），样式表约 28KB、脚本约 8KB，离线双击 `docs/index.html` 即可阅读（所有页面资源同目录，零网络请求）。
- 在线部署：`docs/` 是纯静态产物，直接作为 GitHub Pages 的发布目录即可（Settings → Pages → Deploy from a branch → `main` + `/docs`），**不需要 Actions、不需要额外配置**。`docs/.nojekyll` 由构建脚本生成、必须随产物一起提交——否则 Jekyll 会介入构建；`.nojekyll` 若丢失，重跑 `npm run build` 即可恢复，**不要手工往 `docs/` 里放文件**。
- **改完 `tools/` 的验收动作**：必须重跑 `npm run build`，并用无头 Chrome 按 1440（三栏）、1140（两栏）、880（抽屉）三档宽度截图核对，深色主题另注一次 `data-theme='dark'`；改 `tools/style.css` 后要确认 `docs/style.css` 已同步、且花括号配平（一次丢失的编辑曾让整段 `.tier-title` 规则被静默吞掉）。

## 本地参考资料库（pdf/）使用规范

`pdf/` 内是 12 份软件设计师辅导资料，已用 macOS Vision 框架 OCR 提取为 `pdf/text/*.txt`（已去除品牌水印与页眉页脚；提取工具源码在 `/tmp/swift_ocr/`，会话结束后可能丢失，重建方法见下）。生成讲义时**必须参考、但不得照搬**：

- **每课生成前**：在 `pdf/text/` 中检索该考点相关章节，用「三色笔记」「知识点集锦」核对覆盖范围有无遗漏，用其中标红/点拨内容校准 ★ 高频标注；用「考点自查清单」校准分值占比引用（注意其目录页的分值数字 OCR 有噪声，引用前需渲染原 PDF 对应页核实）。
- **出题时**：「经典 100 题」（带答案解析，已验算无错误）和「案例模拟题合集」（算法填空/UML/数据流图三类必答案例题）用于借鉴真题风格与解题套路；**不得逐字搬运**（版权资料），题干和数字应改写，答案必须自行验算。
- **易错点**：「易混淆知识点」（7 页短文）与我们的「易错点提醒」定位一致，可作为该节素材来源（同样改写后使用）。
- **可忽略**：学习计划、学习手册、学习打卡表是希赛网课配套（用户不看视频），仅在规划复习节奏时可借鉴其「正课→串讲→刷题→冲刺」四阶段思路。
- **OCR 局限**：树形图/E-R 图/状态图等图片内容提取有损，公式下标与特殊符号（∈、∪、F）偶有识别错；引用此类内容前，用 PyMuPDF 把对应页渲染成 PNG 亲眼核对（每份 txt 内的「第 N 页」标记即原 PDF 页码）。
- **git 红线**：`pdf/*.pdf` 原件（大体积二进制 + 版权资料）已在 `.gitignore` 中，任何情况下不得提交；`pdf/text/*.txt` 已去品牌水印、允许随仓库提交，但引用时同样不得在仓库其他文件中出现品牌名，且题目内容仍须改写后使用。
- **OCR 工具重建**（若 `pdf/text/` 与 `/tmp/swift_ocr/` 均丢失）：`pip3 install pymupdf` 渲染页面为 PNG（160dpi 即可），再用 Swift 调 Vision 框架（`import Vision` + `VNRecognizeTextRequest`，识别语言 `["zh-Hans","en-US"]`）按 boundingBox 行聚类排序输出；约 0.9 秒/页。重建提取后须重跑去水印清洗：删除含「非学员版本／学员版本请联系／内部资料／xisaiwang／客服热线／专业的在线职业教育平台」的行，并把剩余行中的「希赛」字样剔除。

### 开源参考项目（`.refs/`）

`pdf/` 之外的第二类参考：**开源项目克隆**，用来补"讲得不周全"的章节、校准讲法与图示。

- 当前有 `hello-algo`（`git clone --depth 1 --filter=blob:none --sparse https://github.com/krahets/hello-algo.git .refs/hello-algo` 后 `git sparse-checkout set docs`，约 43MB，只取 docs/ 文本）。
- **用途**：数据结构/算法类讲义（主要是第 01 课）扩写或自我体检时，对照它的章节划分找自己漏掉的节点（如堆、二叉排序树、分治/DP/贪心/回溯的具体演算），并在讲义末尾加一行「延伸阅读」外链（如 `[hello-algo · 图](https://www.hello-algo.com/chapter_graph/graph/)`）。
- **纪律**：只借结构与讲法，**不整段搬运**；`.refs/` 一律 gitignore（含克隆体与配套脚本），仓库里绝不出现它的副本。
- 仍需以 `pdf/text/` 的真题向材料校准考点范围——开源项目覆盖面比考纲宽，**不考的（如 A*、小顶堆应用）不要往讲义里搬**。

## 每个考点文件的固定模板

文件名：`NN-考点名称.md`（NN 为两位序号）。内部结构必须为以下五部分，顺序不变：

1. **本课导航**：引用大纲中该考点的分值占比、学习目标、与案例题的关联。
2. **讲义（通俗讲解）**：
   - 覆盖大纲中该考点的全部「学习范围」条目，不可遗漏；
   - 语言通俗，多用类比、对比表格、口诀；公式给出推导思路；
   - 高频必考点用 ★ 标注（★★★ 极高频）；
   - 若该考点对应案例题大题（数据流图/SQL/E-R/UML/算法填空/程序填空），单独设「案例题专攻」一节写解题套路 + 代码/答题模板。
3. **课后考题**：约 15 道模拟选择题（覆盖讲义各节）+ 1~2 道案例题风格题。
4. **精华题**：5~8 道历年必考真题风格的浓缩题。
5. **答案与解析**：放在**同一文件最末尾**（用户要求：做题时不易瞄到，且不增加文件数）。逐题给出答案、解析和易错点提醒；计算题要展示完整计算过程。

## 每次任务的执行流程

1. **续写新课**（用户说「下一课」或类似）：
   - 按上面顺序找到下一个未完成的考点；
   - 先按「本地参考资料库（pdf/）使用规范」检索 `pdf/text/` 中该考点章节，核对覆盖范围与高频标注，借鉴题目风格；
   - 按模板生成 `软考学习/NN-xxx.md`；
   - 在 `软考学习/00-总览与进度.md` 中勾掉该考点的 checkbox；
   - 若该考点文件与已学考点有知识联动（如 SQL 与数据结构中的案例联动），在讲义中加链接互引；
   - 重跑 `npm run build` 更新 docs/ 阅读版。
2. **修订已有文件**（用户指出讲得不清楚 / 错题反馈）：
   - 直接修改对应考点文件，讲不清的地方换类比重讲，或补充例题；
   - 题目错误（答案错、题目歧义）必须当场修正答案与解析。
3. **补题**（用户要求加题）：在该考点文件的「精华题」之后追加小节，不破坏原编号。
4. **并行生成多课**（用户要求一次生成多章）：
   - 可为每课派一个子智能体并行执行。子智能体的提示词必须自包含：指明必读文件（AGENTS.md、大纲对应节、需互链的已有课）、该课大纲范围、题目与验算要求、以及**边界约束——只允许写自己的 NN-xxx.md，禁止动 00-总览与进度.md、禁止跑 npm build**；
   - 共享文件统一由主会话在各子智能体全部完成后处理：进度表加链接、重建 docs/；
   - 主会话必须做**跨文件衔接检查**：每课末尾「下一课」指针是否指向正确的下一课（子智能体互不知道彼此产出，指针可能写错）、互链的相对路径是否指向真实存在的文件、全文无品牌名。
5. 全部 12 课完成后，生成 `软考学习/99-考前全真模拟卷.md`，并更新 README 目录树。

## 生成后自检清单（每课写完、交付前必做）

生成讲义的 AI 必须在交付前按此清单自查（这是历史错误教训的固化：第 1 课曾有 5 处答案错误/乱句、第 2 课 4 处，全部源于"边写边算不回头验算"）：

1. **逐题复算**：所有计算题用**第二种方法**反向验证后再落笔定稿——哈夫曼用「非叶节点权值之和 = WPL」交叉验证；遍历还原用「前中后三序互相验算」；McCabe 用「E−N+2」与「判定节点数+1」互验；范式判定从低往高逐级验；拓扑排序/排列数枚举完再数一遍。
2. **草稿痕迹清理**：全文不得残留自我更正、自问自答、边想边写的语句（如「等等，复核：……更正：……」），更正必须消化后重写成干净的结论。
3. **题干自洽**：题目引用的每个属性、实体、名词在题面业务设定中都存在；案例图的已画/缺失数据流清单要与说明文字逐句对得上。
4. **选项唯一可判**：用题面的判别标准（如「枢轴归位 + 两侧大小关系」）逐一检查每个选项，确认恰好一个正确；干扰项必须各自违反一条可检验的性质。
5. **规范对齐**：对照写作规范的五条（动机先行 / 反例前置 / 公式三件套 / 应试动作 / 题干自洽）逐节过一遍，再交付。
6. **跨文件衔接**（并行生成后尤其重要）：文末「下一课」指针指向生成顺序中的下一课而非已存在的课；文中所有相对链接的文件真实存在；进度表已加本课链接。
7. **不重复维护同一份状态**：进度只在 `软考学习/00-总览与进度.md` 勾选、梯队划分只在 `tools/build_html.mjs` 的 `TIERS` 定义、课程顺序只由 `build_html.mjs` 的排序产出——任何"第二份副本"（如本文件里另列的勾选状态、手写的课程清单、样张里写死的目录）都会随时间漂移，改结构时先找唯一真源，改完顺手把副本对齐或删掉。
8. **一句话知识点自足**：补充说明、对比列表里一句话带过的知识点（如循环队列的"标志位法""计数器法"），必须自问"用户只看这句能否学会"；不能的话就地扩成小节（核心矛盾 + ASCII 图示例 + 伪代码 + 口诀），不留"要再追问一遍才能懂"的暗坑。（教训：第 1 课循环队列补充小节一句话带过标志位法，用户追问后才补全。）
9. **不要写页内锚点链接**：构建脚本给标题的 id 是**顺序号**（`h-1`、`h-2`…），新增/删除任何小节都会把后面的序号全部冲掉，所以 md 里**不要写 `](#某某小节)` 这类页内跳转**（点了跳不动）；引用别节一律写成纯文本（如「见第二节 2.5 平衡二叉树」），或改用指向**另一个 md 文件**的相对链接（由构建脚本自动改写成 `.html`）。
10. **标记符号要看"渲染结果"而不是只看内容**：① UML 构造型 `<<include>>`／`<<extend>>`／`<<component>>`／`<<Database>>` 在 md 里**必须包进反引号**——裸写会被浏览器当作未知 HTML 标签，页面上**整段不显示**，而构建脚本**不报错**（第 4 课曾出现 4 处裸写，"发布文章用例的第 6 题选项"因此看不见）；② 生僻 Unicode 符号（如表示半圆的 `◗` U+25D7）在多数中文字体里没有字形，会渲染成豆腐块，注记这类图形一律改用文字描述（"整圆／半圆"）。
11. **长内容不要硬塞进多列表格**：把"角色链""参数串"这类长文本放进多列表格时，短列会被浏览器压到最窄、中文被迫**逐字竖排**（第 4 课 4.4 模式角色表、第 3 课 9.7「三种错误」表都踩过同一坑）。**可直接判定的触发条件：表首列文字 ≤3 字、而同表其他列含长句 → 必然竖排**，此时**不要试图调列宽**，直接改用**分组列表**重写（每条一段、术语用行内代码）。
12. **构建成功不等于渲染正确**：重跑 `npm run build` 之后必须抽查产物再交付。本地工具 `node .refs/preview.mjs "<页码>" <起始h号> <结束h号> <light|dark>`（`.refs/` 不入库）可把任意小节抽成单页预览并截图，脚本已把正文固定成真实中栏宽度（44 rem），列宽与真实页面一致；无头 Chrome 直接截图时 `#h-N` 锚点不稳定，定位一律走预览页。**三个坑**：① 预览前要先把 `docs/style.css`、`docs/app.js` 拷进 `.refs/`，否则预览页无样式；② **区间参数的起止号必须不同**（写成 `54 54` 会生成空白页，看着像渲染失败，其实是没取到内容）；③ `h-N` 是按标题顺序生成的，改完 md 编号会漂移，**每次先重新 grep `id="h-"` 取最新编号**。
13. **教学图示先当题做一遍**：新增或改写 ASCII 图后，必须回头用本节自己讲的规则检查这张图——① **图里结构的形态是否合法**（哈夫曼树不允许出现度为 1 的节点，曾为了演示"内部节点也带权"画出一棵右孩子为空的畸形树，它根本不可能由哈夫曼算法长出来）；② **图里每个数字是否满足本节列出的全部关系**（内部节点权 = 两个孩子之和、非叶节点权之和 = WPL），逐个亲手重算后再落笔；③ **同一小节的多张图必须共用一套口径**——"高度"按层数还是按边数、"路径长度"数边还是数点，一旦图与图之间、图与正文之间口径不一，读者会当成两个矛盾的公式（AVL 的平衡因子曾出现正例按层数、反例按边数，两套混用）；④ **对齐也要核**：等宽字体下斜线偏离 1 格就显得脱节，中文与全角符号按 2 字符宽计——最省事的办法是写个小脚本数出每行关键字符的下标（`[i for i,c in enumerate(line) if c in "/\\"]`），肉眼数空格必错。
    **反例同样要"验伪"**：反例里声称的错误结果必须**真的错、且错在你指出的那一点上**。曾用"第 2 轮挑错会得到根权 16"作反例，重算后发现错误树的根权**同样是 10**，真正暴露问题的是 WPL 20 > 19——若不验伪，这个反例反而会教出错误结论。反例写完后必须按同样方法把它的数值完整算一遍，确认结论站得住再落笔。
14. **元规则**：若本次或用户反馈中出现了清单未覆盖的新型错误，当场把对应检查项追加进本清单——清单本身要随错误类型进化。

## 写作规范

- 全文使用**简体中文**；代码与术语保留英文原文。
- **动机先行**：每个知识点先讲「它解决什么问题 / 为什么需要它」，再讲「怎么操作」。凡是教一种机械步骤（如中缀转后缀、求补码、浮点数规格化），必须紧接着回答初学者最可能的质疑（如「转完运算符不就没意义了吗」），用一小段点透新表示与旧表示的等价性，不能只给步骤不给动机。
- **反例前置**：凡有规则的步骤/记法，要把初学者最容易写错、读错的版本作为反例明确写出（如中缀转后缀写成 `bc+a*`、`abcd+*-`），并演示按规则读错误版本会得到什么错误结果——不能只给正确答案，赌用户不踩坑。
- **公式三件套**：公式必须给出「通用形式 + 不跳步的代入算例 + 手算口诀」；同一公式的多个等价形式（如 C(2n,n)/(n+1) 与 (2n)!/((n+1)!·n!)）必须说明换算关系，防止用户误以为是两个公式；组合数等基础工具若可能被遗忘，就地展开算一遍（如 C(6,3)=(6×5×4)/(3×2×1)=20）。
- **应试动作明确**：每个知识点结尾标注用户该做什么——「背什么（背到什么粒度）」「考场上怎么认题（题干不会出现术语名时要给识别特征）」「哪些考法套公式、哪些考法靠模拟/推导」。用户是闭卷应试，默认卷面不提供任何公式表与提示。
- **题干自洽与选项唯一**：题目中引用的属性、实体、名词必须与该题业务设定完全一致（设定里没有"团长"就不能问"团长名"）；选择题的正确答案必须唯一，干扰项各自违反一条**可检验**的性质，不允许出现多个选项都满足判别标准的情况。
- Markdown 格式；文件开头用引用块（`>`）标注分值地位与学习目标。
- 题目编号在同一小节内连续；选择题给 4 个选项 A–D。
- 答案必须可靠：计算题逐步验算后再落笔；不确定的教材结论宁可不写，不得编造。
- 涉及年份/政策类信息（考试时间、报名、大纲改版）注明「以官方通知为准」。
- 各文件之间的链接一律用相对路径（同目录直接文件名，跨目录 `../`），保证仓库整体可搬移。

## 约束

- 不要把多个考点合并在一个文件里；不要改动已完成课件的编号。
- 不要删除或改写用户的进度勾选状态（只按实际进度勾选）。
- 本仓库未来会推送到 git：不要生成大文件、二进制文件或带绝对路径的链接。
