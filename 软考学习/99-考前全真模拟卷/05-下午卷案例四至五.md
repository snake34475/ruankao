# 99.5 下午卷：案例四至五

给定整数序列 `a[0..n−1]`，求一个最长严格递增子序列（LIS）。`dp[i]` 表示以 `a[i]` 结尾的 LIS 长度，`prev[i]` 保存该子序列中 `a[i]` 的前驱下标。请补全伪代码：

```c
for (int i = 0; i < n; i++) {
    dp[i] = /* (1) */;
    prev[i] = -1;
    for (int j = 0; j < i; j++) {
        if (/* (2) */ && dp[j] + 1 > dp[i]) {
            dp[i] = /* (3) */;
            prev[i] = j;
        }
    }
}

int best = 0;
for (int i = 1; i < n; i++) {
    if (/* (4) */) best = i;
}

int k = best;
while (k != -1) {
    output_reverse(a[k]);
    k = /* (5) */;
}
```

**问题 1（8 分）**：填出空（1）～（5）。其中（1）～（3）每空 2 分，（4）～（5）每空 1 分。

**问题 2（3 分）**：说明为什么状态转移只检查 `j<i` 且 `a[j]<a[i]`。

**问题 3（2 分）**：给出算法的时间复杂度和额外空间复杂度。

**问题 4（2 分）**：对序列 `[3,1,2,5,4,7]`，按上述“只有长度严格变大才更新前驱”的代码，给出 LIS 长度和反向输出后得到的一条正向 LIS。

## 案例五 Java 程序设计与设计模式（15 分）

库存系统希望在库存变化时自动通知多个订阅者。补全下面的 Java 代码：

```java
import java.util.ArrayList;
import java.util.List;

interface StockObserver {
    void update(int stock);
}

class Inventory {
    private int stock;
    private final List<StockObserver> observers = new ArrayList<>();

    public void attach(StockObserver observer) {
        /* (1) */;
    }

    public void setStock(int stock) {
        /* (2) */;
        /* (3) */;
    }

    private void notifyObservers() {
        for (StockObserver observer : observers) {
            /* (4) */;
        }
    }
}

class EmailAlert /* (5) */ {
    @Override
    public void update(int stock) {
        if (stock < 10) {
            System.out.println("Low stock: " + stock);
        }
    }
}
```

**问题 1（8 分）**：填出空（1）～（5）。（1）～（4）每空 1.5 分，（5）2 分。

**问题 2（4 分）**：指出所用设计模式，并说明 Inventory、StockObserver、EmailAlert 分别对应哪个角色。

**问题 3（3 分）**：与把所有通知逻辑直接写进 Inventory 相比，这种设计的主要优点是什么？

---

